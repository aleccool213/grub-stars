# grub stars Environment Configuration
# Copy this file to .env and fill in your values

# ===========================================
# API Keys (for restaurant data sources)
# ===========================================
YELP_API_KEY=
GOOGLE_API_KEY=
TRIPADVISOR_API_KEY=

# Optional: Instagram and TikTok (planned features)
# INSTAGRAM_API_KEY=
# TIKTOK_API_KEY=

# ===========================================
# Authentication (optional)
# ===========================================
# Set a password to protect the indexing feature
# If not set, anyone can index locations (fine for local dev)
# For production, set this to restrict access to friends/family
GRUB_STARS_AUTH_PASSWORD=

# Session secret for secure cookies
# Generate with: openssl rand -hex 32
# If not set, a random secret is generated on each server restart
# (which will invalidate existing sessions)
SESSION_SECRET=

# ===========================================
# Database (optional)
# ===========================================
# Override the default database location (~/.grub_stars/grub_stars.db)
# GRUB_STARS_CONFIG_DIR=/path/to/config
